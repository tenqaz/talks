apiVersion: batch/v1
kind: Job
metadata:
  name: echo-job

spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - image: mirrors.sangfor.com/busybox
        name: echo-job
        command: ["/bin/echo"]
        args: ["hello", "world"]

